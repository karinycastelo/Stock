<p id="notice"><%= notice %></p>

<div class="container">
  <div class="row">
    <div class="column" style="text-align: left; margin-left: -15px;">
      <h3>Setores</h3>
    </div>
    <div class="column" style="text-align: right;">
      <h4><%= link_to 'Novo Setor', new_sector_path %></h4>
    </div>
  </div>

  <div class="row">

    <table class="table">
      <thead>
        <tr>
          <th>Nome</th>
          <th>Endereço</th>
          <th>Secretaria</th>
          <th colspan="3"></th>
        </tr>
      </thead>

      <tbody>
        <% @sectors.each do |sector| %>
        <tr>
          <td><%= sector.name %></td>
          <td><%= sector.address %></td>
          <td><%= sector.secretary.name %></td>
          <td><%= link_to 'Mostrar', sector %></td>
          <td><%= link_to 'Editar', edit_sector_path(sector) %></td>
          <td><%= link_to 'Excluir', sector, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
</div>
