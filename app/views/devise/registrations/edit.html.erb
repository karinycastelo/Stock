<div class="container">
  <div class="card">
    <div class="card-content">
      <%# <h3>Editar Conta %>
        <!-- <%= resource_name.to_s.humanize %> -->
      <%# </h3> %>
      <%= form_for(resource, :as => resource_name, url: registration_path(resource_name), html: { method: :put, role: 'form'}) do |f| %>
        <%= devise_error_messages! %>
        <div class="form-group">
          <%= f.label :name, "Nome" %>
          <%= f.text_field :name, autofocus: true, class: 'form-control' %>
        </div>
        <div class="form-group">
          <%= f.label :email %>
          <%= f.email_field :email, class: 'form-control' %>
          <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
            <div>Aguardando confirmação para: <%= resource.unconfirmed_email %></div>
          <% end %>
        </div>
        <div class="card">
          <div class="card-content">
            <p>Deixe em branco se não quiser alterar sua senha.</p>
            <div class="form-group">
              <%= f.label :password, "Senha" %>
              <%= f.password_field :password, autocomplete: 'off', class: 'form-control' %>
            </div>
            <div class="form-group">
              <%= f.label :password_confirmation, "Confirmação de Senha" %>
              <%= f.password_field :password_confirmation, class: 'form-control' %>
            </div>
          </div>
        </div>
          <br>
        <div class="card">
          <div class="card-content">
            <p>Você deve informar sua senha para alterar dados.</p>
            <div class="form-group">
              <%= f.label :current_password, "Senha Atual" %>
              <%= f.password_field :current_password, class: 'form-control' %>
            </div>
          </div>
        </div>
        <br>

        <%= f.submit 'Atualizar', class: 'waves-effect waves-light btn blue-grey darken-1 right' %>
      <% end %>
  <%= button_to "Cancelar minha conta", registration_path(resource_name), data: { confirm: "Você tem certeza?" }, method: :delete, class: 'waves-effect waves-light btn blue-grey darken-1' %>
</div>
<%# <div class="authform"> %>
<%# <h3>Cancelar Conta</h3>
<p style="color: #fff;">Insatisfeito? Ficaremos triste por vê-lo sair.</p> %>
<%# <%= button_to "Cancelar minha conta", registration_path(resource_name), data: { confirm: "Você tem certeza?" }, method: :delete, class: 'waves-effect waves-light btn' %>
<%# </div> %>
