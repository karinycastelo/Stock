<p id="notice"><%= notice %></p>
<div class="container">
  <div class="row">
    <div class="column" style="text-align: left; margin-left: -15px;">
      <h3>Secretarias</h3>
    </div>
    <div class="column" style="text-align: right;">
      <h4><%= link_to 'Nova Secretaria', new_secretary_path %></h4>
    </div>
  </div>

  <div class="row">

    <table class="table">
  <thead>
    <tr>
      <th>Nome</th>
      <th>Endereço</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @secretaries.each do |secretary| %>
      <tr>
        <td><%= secretary.name %></td>
        <td><%= secretary.address %></td>
        <td><%= link_to 'Mostrar', secretary %></td>
        <td><%= link_to 'Editar', edit_secretary_path(secretary) %></td>
        <td><%= link_to 'Excluir', secretary, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
