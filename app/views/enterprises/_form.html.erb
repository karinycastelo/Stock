<%= simple_form_for(@enterprise, html: { class: 'form-vertical' }) do |f| %>
<% if enterprise.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(enterprise.errors.count, "error") %> prohibited this item from being saved:</h2>
  <ul>
    <% enterprise.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>

<div class="form-inputs">
  <div class="row-sm">
      <%= f.input :name, label: "Nome" %>
  </div>
  <div class="row-sm">
    <%= f.input :description, label: "Descrição" %>
  </div>
</div>
<br>

<div class="form-actions">
  <%= f.button :submit, 'Criar Empresa' %>
</div>
<% end %>
